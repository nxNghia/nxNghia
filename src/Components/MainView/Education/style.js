import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
    root: {
        marginTop: 150,
        marginBottom: 150,
        paddingLeft: 100,
        paddingRight: 100
    },
    root_smaller: {
        marginTop: 150,
        marginBottom: 150
    }
}))